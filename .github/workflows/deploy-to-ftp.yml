on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: wesmeindertsma/FTP-Deploy-Action@4.3.2
      with:
        server: ftp.mttandheelkunde.nl
        username: meindertsma@mttandheelkunde.nl
        password: ${{ secrets.ftp_password }}
